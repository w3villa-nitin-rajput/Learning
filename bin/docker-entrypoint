#!/bin/bash -e

echo "Running migrations..."
./bin/rails db:migrate

# This forces seeds to run every time. 
# Ensure your seeds.rb uses find_or_create_by to avoid duplicates!
echo "Running seeds..."
./bin/rails db:seed

exec "${@}"